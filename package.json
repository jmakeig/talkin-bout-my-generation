{
	"name": "talkin-bout-my-generation",
	"version": "0.1.0",
	"description": "",
	"main": "index.js",
	"bin": "./cli.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"clean": "rm -rf ./data/ && mkdir -p ./data/customers ./data/products ./data/orders",
		"generate": "npm run clean && npx . entities --customers=5033 --products=602 --orders=20755"
	},
	"keywords": [],
	"author": "Justin Makeig <jmpublic+github@makeig.com> (https://github.com/jmakeig)",
	"license": "Apache-2.0",
	"dependencies": {
		"docopt": "^0.6.2",
		"faker": "^4.1.0",
		"prettier": "^1.19.1",
		"prob.js": "^1.0.6"
	}
}
